import { MessageCard } from './MessageCard';

export const MessageList = () => {
    return (
        <div>
            <MessageCard />
            <MessageCard />
            <MessageCard />
        </div>
    );
}
